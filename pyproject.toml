[project]
name = "test-remote-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Kaustubhd651", email = "92288567+Kaustubhd651@users.noreply.github.com" }
]
requires-python = ">=3.10"
dependencies = [
    "aiosqlite>=0.22.1",
    "fastmcp>=4.0.3",
]

[project.scripts]
test-remote-server = "test_remote_server:main"

[build-system]
requires = ["uv_build>=0.12.11,<0.13.0"]
build-backend = "uv_build"
